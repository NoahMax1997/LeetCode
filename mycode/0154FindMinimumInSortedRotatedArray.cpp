#include <iostream>
#include <map>
#include <set>
#include <vector>
#include <stack>
#include <algorithm>
using namespace std;
class Solution {
public:
    int ret=0;
    void binarySearch(vector<int>& nums,int left,int right,int &ret){
        int mid=(left+right)/2;
        if(nums[mid]>nums[left]){
            if()
        }else{

        }
    }
    int findMin(vector<int>& nums) {
    int findMin(vector<int>& nums) {
        
    }
};
int main (){
		
	return 0;
}
